function validarUsuario() {

    /*var email = document.formLogin.mail.value;
    email = email.trim();

    var usuario = document.formLogin.username.value;
    usuario = usuario.trim();

    var contraseña = document.formLogin.password.value;
    contraseña = contraseña.trim();

    var name = document.formLogin.nombre.value;
    name = name.trim();

    var lastname = document.formLogin.apellido.value;
    lastname = lastname.trim();

    if (name === null || (name.length < 2)) {
        alert("Complete el campo de Nombre, minimo requerido, 2 caracteres.");
        return false;
    }

    if (name.length > 30) {
        alert("El campo de Nombre tiene como maximo permitido 30 caracteres.");
        return false;
    }

    if (!isNaN(name)) {
        alert("El campo de Nombre no permite numeros.");
        return false;
    }

    if (lastname === null || lastname.length < 2) {
        alert("Complete el campo de Apellido, minimo requerido, 2 caracteres.");
        return false;
    }

    if (lastname.length > 30) {
        alert("El campo de Nombre tiene como maximo permitido 30 caracteres.");
        return false;
    }

    if (!isNaN(lastname)) {
        alert("El campo de Apellido no permite numeros.");
        return false;
    }

    if (usuario === null || usuario.length < 3) {
        alert("Complete el campo de Usuario, minimo requerido, 3 caracteres.");
        return false;
    }

    if (usuario.length > 30) {
        alert("El campo de Usuario tiene como maximo permitido 30 caracteres.");
        return false;
    }

    if (contraseña === null || contraseña.length < 6) {
        alert("Complete el campo de Contraseña, minimo requerido, 6 caracteres.");
        return false;
    }

    if (contraseña.length > 20) {
        alert("El campo de Contraseña tiene como maximo permitido 20 caracteres.");
        return false;
    }

    if (email === null || email.length < 4) {
        alert("Complete el campo Mail.");
        return false;
    }
    */
    alert("Datos ingresados correctamente");
    return true;
}